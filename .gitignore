# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage
coverage.xml

# Warehouse. Local only, tens of GB, never committed.
warehouse/
*.duckdb
*.duckdb.wal
raw/

# dbt
transform/target/
transform/dbt_packages/
transform/logs/
logs/

# Node
node_modules/
web/.next/
web/out/
web/.turbo/

# The shipped layer IS committed. GitHub Pages serves from the repository, so
# the demo works on first click without a build step, and the size budget in
# config/cityflow.yml is what keeps that honest. The warehouse it was built
# from is not committed and never will be.

# OS
.DS_Store

# dbt writes this per user
transform/.user.yml

# Recorded frames are an intermediate, the animation is the artifact
web/tests/demo-frames/
